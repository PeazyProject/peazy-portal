import { ReplaceNullWithPipe } from './replace-null-with.pipe';

describe('ReplaceNullWithPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceNullWithPipe();
    expect(pipe).toBeTruthy();
  });
});
