import { OptionsMappingPipe } from './options-mapping.pipe';

describe('OptionsMappingPipe', () => {
  it('create an instance', () => {
    const pipe = new OptionsMappingPipe();
    expect(pipe).toBeTruthy();
  });
});
