import { DataTableSettingDirective } from './data-table-setting.directive';

describe('DataTableSettingDirective', () => {
  it('should create an instance', () => {
    const directive = new DataTableSettingDirective();
    expect(directive).toBeTruthy();
  });
});
